// O programa “ao quadrado” imprime os quadrados dos números de um a nove, separando cada expressão 
// em uma linha.

for (let index = 0; index < 9; index++) {
    var resultado = index * index;
    console.log(index, "^", index, "=", resultado);
}

